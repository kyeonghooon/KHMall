<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <property name="CONSOLE_PATTERN"
    value="%d{HH:mm:ss.SSS} %-5level %logger{36} - %msg%n"/>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>${CONSOLE_PATTERN}</pattern>
    </encoder>
  </appender>

  <property name="AUDIT_PATTERN"
    value="%d{yyyy-MM-dd HH:mm:ss.SSS} [userId=%X{userId}] %msg%n"/>
  <appender name="AUDIT_FILE" class="ch.qos.logback.core.FileAppender">
    <file>logs/test_audit.log</file>
    <encoder>
      <pattern>${AUDIT_PATTERN}</pattern>
    </encoder>
  </appender>

  <logger name="com.khmall.log.audit.AuditLogger" level="INFO" additivity="false">
    <appender-ref ref="AUDIT_FILE"/>
  </logger>

  <!-- 에러 메시지만 남기는 전용 appender -->
  <appender name="ERROR_ONLY_FILE" class="ch.qos.logback.core.FileAppender">
    <file>logs/test_error_message_only.log</file>
    <append>true</append>
    <encoder>
      <pattern>${CONSOLE_PATTERN}</pattern>
    </encoder>
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
      <level>ERROR</level>
      <onMatch>ACCEPT</onMatch>
      <onMismatch>DENY</onMismatch>
    </filter>
  </appender>

  <root level="INFO">
    <appender-ref ref="STDOUT"/>
    <appender-ref ref="ERROR_ONLY_FILE"/>
  </root>
</configuration>
